# Task 6 — GitHub Actions instead of Travis

**✅ done 2026-09-02** (`9.0.0-dev.6`, commit 6414c6b; first real CI
run pending the next push to GitHub)

- `.travis.yml` deleted.
- New `.github/workflows/ci.yml`: on every push/PR, shell and JS
  syntax checks plus a full 3-arch build matrix (armv7l with patchelf)
  with 14-day artifact upload — CI finally runs actual checks.
- `.github/workflows/build.yml` (release, `workflow_dispatch`)
  rewritten: checkout@v4 / setup-node@v4 (Node 24), no obsolete apt
  packages (`libavahi-compat-libdnssd-dev`, `libudev-dev`), runs
  `build.sh`, tags `v<package.json version>` via the release action
  and creates a draft prerelease with `dist/*` and `RELEASE_BODY.md`.
- `build_release_body.sh`: armv6l/i686/Travis sections removed,
  OpenCCU naming, host jq replaced by node, module-version table
  generated by a node scan; verified locally against a real build.
- `update_change_history.js` rewritten on global `fetch` (was the dead
  `request` package); wiki push keeps using `GITHUB_OAUTH_TOKEN`, fed
  from `secrets.GITHUB_TOKEN` in the workflow.
- `build.sh` no longer installs devDependencies (the release scripts
  are dependency-free now).
